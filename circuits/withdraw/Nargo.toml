[package]
name = "apt_withdraw"
type = "bin"
authors = ["apt-mvp"]

[dependencies]
poseidon = { git = "https://github.com/noir-lang/poseidon", tag = "v0.1.1" }
common_merkle = { path = "../common_merkle" }
